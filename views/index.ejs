<!DOCTYPE html>
<html lang="en">
  <head>
      <title><%= title %></title>
      <link rel='stylesheet' href='/stylesheets/style.css' />
      <meta charset="utf-8">
      <meta name="viewport" content="width=device-width, initial-scale=1">
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
      <style>
        body {
          background-color: #f8f9fa;
        }
        .container {
          max-width: 960px;
        }
        .lh-lg {
          line-height: 1.75 !important;
        }
        .table > tbody > tr > td {
          vertical-align: middle;
        }
        .table .reference a {
          color: inherit;
          text-decoration: none;
        }
        .table .reference a:hover {
          color: #0d6efd;
        }
      </style>
  </head>
  <body>
    <div class="container py-5">
      <h2 class="pb-3 mb-4 border-bottom"><%= title %></h2>         
      <div class="table-responsive">
        <table class="table table-hover align-middle">
          <thead>
            <tr>
              <th scope="col">#</th>
              <th scope="col">Terminology</th>
              <th scope="col">Description</th>
              <th scope="col">Reference</th>
            </tr>
          </thead>
          <tbody>
            <% for (var i = 0; i < glossery.length; i++) { %>
              <tr>
                <th scope="row"><%= i+1 %></th>
                <td><%= glossery[i].terminology %></td>
                <td class="lh-lg"><%= glossery[i].description %></td>
                <td class="reference"><a href="<%= glossery[i].reference.split('Available: ')[1] %>" target="_blank"><%= glossery[i].reference.split('Available: ')[0] %>Visit link</a></td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
